# 环境变量配置示例
# 复制此文件为 .env.development 或 .env.production

# 开发环境和生产环境：空的 baseURL，使用相对路径
# 请求 /system/config 会被 Nginx 的 / 位置处理或直接转发
VUE_APP_BASE_API=
VUE_APP_WS_URL=/im

# Nginx 配置：
# 所有 /api/ 请求被重写为直接请求（无 /api 前缀）
# 或者 location /api/ 代理到后端时移除 /api 前缀
